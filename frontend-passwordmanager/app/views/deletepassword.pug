extends template

block content
  p insert the 'username' and 'password' to delete it. warning there will be no way to recover it.
  div#error-message

  form#delete-password(data-id=index, data-appName=appName)
    input(type='text' name='username' placeholder='username...')
    input(type='text' name='hash' placeholder='password...')
    button(type="submit") submit