extends template

block content
  div#error-message
  form#login-form
    h1 LOGIN
    input#username-login(type="text" name="username" placeholder="username")
    input#hash-login(type="password" name="hash" placeholder="hash")
    button(type="submit") submit