extends template

block content
  p insert the new 'username' and new 'password' to update it. warning there will be no way to recover it.
  div#error-message

  form#update-password(data-id=index, data-appName=appName)
    input(type='text' name='username' placeholder='username...')
    input(type='text' name='hash' placeholder='password...')
    button(type="submit") submit